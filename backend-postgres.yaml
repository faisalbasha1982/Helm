postgres:
    config:
        data:
            - key: POSTGRES_DB
              value: dbname
            - key: POSTGRES_USER
              value: postgres
            - key: POSTGRES_PASSWORD
              value: hello